{% extends 'master' %}

{% block content %}
  <div>
    <h3>Create Task</h3>
    {{ form.open({action: 'TaskController.store', params: {project_id: project_id}, files:true }) }}

      {{ csrfField }}

      {% include "./field.njk" %}

      {% include "./add_more.njk" %}

      {{ form.submit('Save', 'save', { class: 'btn btn-warning' }) }}

    {{ form.close() }}
  </div>
{% endblock %}
